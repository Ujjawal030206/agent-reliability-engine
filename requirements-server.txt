# Runtime dependencies for the FastAPI dashboard only (server.py + static/).
#
# Deliberately excludes streamlit/pandas and their transitive weight
# (pyarrow, numpy, altair, pydeck) — server.py never imports them. Keeps the
# hosted build small and fast on a free tier.
#
# For local development of BOTH front ends, use requirements.txt instead.

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
python-dotenv>=1.0
openai>=1.40        # every OpenAI-compatible provider: Groq, Gemini, OpenRouter, ...
anthropic>=0.34     # only used when LLM_PROVIDER=anthropic
